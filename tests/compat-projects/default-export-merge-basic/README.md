# default-export-merge-basic

A default-exported declaration that merges with another declaration of its name is TS2652 on each; exported and local halves of one merge are TS2395 (tsc's `checkExportsOnMergedDeclarations`).
