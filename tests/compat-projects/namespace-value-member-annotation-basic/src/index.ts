import { shapes } from "./lib";

export const count: string = shapes.count;
export const name: number = shapes.Box.name;
export const label: number = shapes.nested.label;
export const missing = shapes.Box.nonExistentMember;
