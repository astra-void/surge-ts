# verbatim-module-syntax-commonjs

`verbatimModuleSyntax` in a CommonJS module: a top-level exported value
declaration (TS1287), `export default <expression>` and a dynamic `import()`
(TS1295), and `export default` in a `.cts` file (TS1286).
