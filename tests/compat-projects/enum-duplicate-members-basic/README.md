# enum-duplicate-members-basic

An enum member name declared twice — within one declaration or across the
merged declarations of one enum — is TS2300 at every declaration, named as
written (`'"a"'` for a quoted member). surge reported none.
