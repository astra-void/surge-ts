# generic-type-argument-range-basic

A type reference whose argument count falls outside the generic's range reports at the reference: TS2314 when every type parameter is required, TS2707 with the range when some have defaults. Classes and interfaces are named with their parameters, aliases by name alone.
