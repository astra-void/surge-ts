export interface Registry<T> {
  get(key: string): T | undefined;
  set(key: string, value: T): void;
}

export declare function createRegistry<T>(): Registry<T>;

export declare const defaults: { retries: number };
