# super-follower-grammar-basic

The parser's TS1034: `super` not followed by `(`, `.` or `[`, reported on
the token that came instead. It is a parse error, so the file carries nothing
else for tsc to report.
