# variance-annotation-placement-basic

`in`/`out` variance annotations belong on the type parameters of a class,
interface, or type alias (TS1274 elsewhere), and on a type alias only when it
names an object, function, constructor, or mapped type (TS2637).
