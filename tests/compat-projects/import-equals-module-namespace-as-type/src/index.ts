import WinJS = require("./classes");
var x = (w1: WinJS) => { };
function z(w3: WinJS) { }
let promise: WinJS.Promise = WinJS.Promise.timeout(1);

import V = require("./values");
let valuesAsType: V;
const value: number = V.v;

import N = require("./namespace-target");
let namespaceAsType: N;
let member: N.J = { j: 1 };

const moduleObject = WinJS;
let constAsType: moduleObject;
