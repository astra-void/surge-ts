# type-predicate-parameter-basic

`checkTypePredicate`: a predicate naming no parameter of its signature
(TS1225) or a name declared inside a binding-pattern parameter (TS1230),
across functions, arrows, function types, methods and call signatures; and a
predicate anywhere but such a signature's return type — an accessor, a
constructor type, a construct signature — is TS1228.
