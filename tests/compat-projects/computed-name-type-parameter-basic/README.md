# computed-name-type-parameter-basic

The binder's TS2467: a computed member name referring to a type parameter
of the class or interface that owns the member. A generic arrow inside the
name declaring the same name shadows it.
