# module-keyword-namespace-basic

An identifier-named declaration written with the `module` keyword is TS1540
on its name (`checkModuleDeclaration`), on every segment of a dotted name;
`namespace` and string-named `module` declarations are fine.
