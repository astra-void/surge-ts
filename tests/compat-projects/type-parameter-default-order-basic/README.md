# type-parameter-default-order-basic

A type parameter without a default may not follow one with a default
(TS2706), in any type parameter list: interfaces, aliases, classes,
functions and arrows. surge did not check the order.
