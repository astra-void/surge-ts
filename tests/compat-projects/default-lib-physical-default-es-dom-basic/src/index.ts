const m = new Map<string, number>();
m.set("a", 1);
const p: Promise<number> = Promise.resolve(1);
const id = JSON.stringify({ ok: true });
const n = Number("3");
const arr = Array.from(new Uint8Array(2));
