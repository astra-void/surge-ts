# numeric-literal-forms-valid

A lone `0`, a `0` followed by a fraction or an exponent, and the `0o`/`0x`/`0b`
prefixed forms are not legacy octals or leading-zero decimals; nothing gates the
semantic TS2322.
