# rest-element-non-array-basic

A tuple rest element whose type is not array-like is TS2574 on the element
(tsc's checkTupleType), once per tuple. A constrained type parameter answers
through its constraint.
