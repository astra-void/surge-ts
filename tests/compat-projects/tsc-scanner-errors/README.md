# tsc-scanner-errors

The errors typescript-go's scanner reports while tokenizing: unterminated
literals and comments, malformed numeric literals and separators, invalid and
octal escapes, invalid characters, merge-conflict markers and a misplaced `#!`.
