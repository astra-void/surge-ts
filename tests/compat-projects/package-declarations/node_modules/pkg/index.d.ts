export type Id = string;

export interface User {
  id: Id;
  name: string;
}

export declare function makeUser(name: string): User;
export declare const version: string;

declare const _default: typeof makeUser;
export default _default;
