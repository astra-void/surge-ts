declare function log(message: string): void;

declare namespace log {
  const level: number;
  function child(name: string): typeof log;
  interface Options {
    pretty?: boolean;
  }
  namespace formats {
    const json: string;
  }
}

export = log;
