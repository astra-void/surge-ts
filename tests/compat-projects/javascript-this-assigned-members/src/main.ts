const model = new Model();
const id: string | number = model.id;
const count: number = model.count;
const label: string = model.label;
const cache: any[] | undefined = model.cache;
const missing: number = model.missing;
const instances: number | undefined = Model.instances;
const pending = model.pending;
