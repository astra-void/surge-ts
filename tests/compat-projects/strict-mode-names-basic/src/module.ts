export {};

function params(eval: number, ...arguments: any[]) {
  eval++;
  --arguments;
  eval += 1;
}
function destructured({ eval }: { eval: number }) {}
declare function ambient(eval: number): void;
try {
} catch (eval) {}
let { a: arguments2, b: eval } = { a: 1, b: 2 };

class Methods {
  eval() {
    let eval = 1;
  }
}

let implements = 1;
function withStatic(static: number) {}
class Packaged {
  m(package: number) {}
}
var yield = 1;
interface public {}
const holder = { static: 1 };
holder.static;
