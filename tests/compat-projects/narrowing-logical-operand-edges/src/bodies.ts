export {};
function andFalse(x: string | number | boolean, other: boolean) {
  if (typeof x !== "string" && typeof x !== "number") {
    const b: boolean = x;
  } else {
    const sn: string | number = x;
    const bad: string = x;
  }
  const r = typeof x !== "string" && typeof x !== "number" ? x : x;
  const t: string | number | boolean = r;
  if (typeof x !== "string" && other) {
  } else {
    const whole: string | number | boolean = x;
  }
}

function orTrue(x: string | number | boolean) {
  if (typeof x === "string" || typeof x === "number") {
    const sn: string | number = x;
  }
  const sn = typeof x === "string" || typeof x === "number" ? x : 0;
  const u: string | number = sn;
}
