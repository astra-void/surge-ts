export type LocalUser = {
  id: string;
};

export declare const localValue: number;

export type Broken = Missing;

export interface BrokenShape {
  kind: Missing;
}

export declare function broken(): Missing;
