# jsx-factory-and-type-argument-arity

A JSX tag with no `React` in scope for the `jsx: react` factory (TS2874), a
fragment likewise (TS2879), and type arguments whose count falls between
what an overload group's members take without matching any (TS2743).
