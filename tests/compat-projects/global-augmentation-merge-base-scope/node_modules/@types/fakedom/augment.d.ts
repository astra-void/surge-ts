export {};

// Module-local meaning of a name the global interface's members refer to. It is
// reachable only through *this* file's resolution scope, which is exactly what
// must not become the merged global interface's scope.
type DomNodeDetail = "module-local-meaning";

declare global {
  interface DomNode {
    readonly augmented: boolean;
  }
}
