export const kind: "element" = domRoot.detail.kind;
export const nodeName: string = domRoot.nodeName;
export const augmented: boolean = domRoot.augmented;

export const wrong: number = domRoot.detail.kind;
