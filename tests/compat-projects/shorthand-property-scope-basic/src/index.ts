declare const known: number;
const nearlyRight = 1;

export const plain = { known, missingName };

export const suggestion = { nearlyRigth };

type Shape = { known: number };
export const excess: Shape = { known, alsoMissing };

export const fine = { known, nearlyRight };
