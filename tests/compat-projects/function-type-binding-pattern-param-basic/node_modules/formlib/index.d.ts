export type FieldBag = {
    value: string;
    onChange: (next: string) => void;
};
export type CtrlProps<T extends FieldBag = FieldBag> = {
    render: ({ field }: { field: T }) => string;
} & {
    name: string;
};
export declare function Controller<T extends FieldBag = FieldBag>(props: CtrlProps<T>): string;
