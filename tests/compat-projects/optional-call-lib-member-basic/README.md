# optional-call-lib-member-basic

A member of a built-in receiver that surge's own member tables do not list is
read from the global interface the configured lib declares, and an optional
call (`maybe?.anchor(...)`) looks it up the same way a plain call does rather
than reporting it missing.
