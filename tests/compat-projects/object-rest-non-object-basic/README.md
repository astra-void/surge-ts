# object-rest-non-object-basic

An object rest binding needs an object source (isValidSpreadType): a
primitive, unknown, or a union with a primitive member is TS2700 on the rest
name, while an array, a type parameter or an object-constrained one is fine.
