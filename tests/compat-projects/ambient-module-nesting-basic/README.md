# ambient-module-nesting-basic

`checkModuleDeclaration`: a string-named module declared inside a namespace,
or inside an ambient module of an external module file (where it is not an
augmentation), is TS2435 on its name.
