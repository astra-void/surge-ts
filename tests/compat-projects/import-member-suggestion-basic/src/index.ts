import { createClint, helperFunctin, type Optons, missingThing } from "./lib";
import { unrelated } from "./plain";

export { createClint, helperFunctin, missingThing, unrelated };
export type Alias = Optons;
