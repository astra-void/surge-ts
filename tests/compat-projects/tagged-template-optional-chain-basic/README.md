# tagged-template-optional-chain-basic

`checkGrammarTaggedTemplateChain`'s TS1358: a tagged template whose tag is
part of an optional chain, reported on the template. Parentheses around the
chain end it.
