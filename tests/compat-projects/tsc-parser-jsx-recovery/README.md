# tsc-parser-jsx-recovery

JSX recovery in typescript-go's parser: unclosed and mismatched elements and
fragments, a JSX text `}`/`>` the scanner flags, an attribute value that is
neither a string nor an expression, and adjacent root elements.
