# reference-lib-directive-leading-only-basic

A triple-slash directive counts only in the comments that lead a file, before
its first statement. One written after code is an ordinary comment, so the
program keeps the target's libs and `BigInt` is TS2583.
