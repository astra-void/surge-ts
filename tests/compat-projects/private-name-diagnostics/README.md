# private-name-diagnostics

Private names (`#x`): an accessibility modifier on one (TS18010), a read
through a nested class that shadows the name (TS18014), `delete` of one
(TS18011), and a read outside the declaring class (TS18013).
